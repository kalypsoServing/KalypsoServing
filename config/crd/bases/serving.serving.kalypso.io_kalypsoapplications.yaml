---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: kalypsoapplications.serving.serving.kalypso.io
spec:
  group: serving.serving.kalypso.io
  names:
    kind: KalypsoApplication
    listKind: KalypsoApplicationList
    plural: kalypsoapplications
    singular: kalypsoapplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.projectRef
      name: Project
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.activeModels
      name: Models
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          KalypsoApplication is the Schema for the kalypsoapplications API
          It serves as the entry point and router for traffic through Istio Gateway
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of KalypsoApplication
            properties:
              description:
                description: Description provides a description of the application
                type: string
              projectRef:
                description: ProjectRef is the reference to parent KalypsoProject
                type: string
              source:
                description: Source defines the Git repository configuration
                properties:
                  branch:
                    default: main
                    description: 'Branch is the target branch (default: main)'
                    type: string
                  buildWorkflow:
                    description: BuildWorkflow is the path to GitHub Action workflow
                      file
                    type: string
                  gitRepository:
                    description: GitRepository is the Git repository URL
                    type: string
                required:
                - gitRepository
                type: object
              storage:
                description: Storage defines common storage/secret configuration for
                  all TritonServers
                properties:
                  region:
                    description: Region is the cloud region for storage
                    type: string
                  secretName:
                    description: SecretName is the name of secret containing credentials
                    type: string
                required:
                - secretName
                type: object
            required:
            - projectRef
            type: object
          status:
            description: status defines the observed state of KalypsoApplication
            properties:
              activeModels:
                description: ActiveModels is the count of active models under this
                  application
                type: integer
              conditions:
                description: Conditions represent the current state of the KalypsoApplication
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              gatewayEndpoint:
                description: GatewayEndpoint is the Istio Gateway endpoint URL
                type: string
              phase:
                description: 'Phase represents the current phase of the application:
                  Pending, Ready, Failed'
                enum:
                - Pending
                - Ready
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
