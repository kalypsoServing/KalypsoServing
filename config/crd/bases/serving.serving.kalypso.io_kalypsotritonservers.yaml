---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: kalypsotritonservers.serving.serving.kalypso.io
spec:
  group: serving.serving.kalypso.io
  names:
    kind: KalypsoTritonServer
    listKind: KalypsoTritonServerList
    plural: kalypsotritonservers
    singular: kalypsotritonserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.applicationRef
      name: Application
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.availableReplicas
      name: Available
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          KalypsoTritonServer is the Schema for the kalypsotritonservers API
          It deploys and manages NVIDIA Triton Inference Servers
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of KalypsoTritonServer
            properties:
              applicationRef:
                description: ApplicationRef is the reference to parent KalypsoApplication
                type: string
              networking:
                description: Networking defines service port configuration
                properties:
                  grpcPort:
                    default: 8001
                    description: 'GrpcPort is the gRPC port (default: 8001)'
                    format: int32
                    type: integer
                  httpPort:
                    default: 8000
                    description: 'HTTPPort is the HTTP port (default: 8000)'
                    format: int32
                    type: integer
                  metricsPort:
                    default: 8002
                    description: 'MetricsPort is the metrics port (default: 8002)'
                    format: int32
                    type: integer
                type: object
              observability:
                description: Observability defines observability configuration for
                  logging, tracing, profiling, and metrics
                properties:
                  collectorEndpoint:
                    description: |-
                      CollectorEndpoint is the unified endpoint for pushing signals (primarily tracing)
                      Used as the destination for OTLP traces
                    type: string
                  enabled:
                    default: false
                    description: Enabled enables observability features globally
                    type: boolean
                  logging:
                    description: Logging defines Grafana Loki logging configuration
                    properties:
                      enabled:
                        default: true
                        description: Enabled enables logging configuration
                        type: boolean
                      level:
                        default: INFO
                        description: |-
                          Level controls application-level log verbosity
                          Maps to Triton's --log-verbose / --log-info / --log-error flags
                        enum:
                        - INFO
                        - WARNING
                        - ERROR
                        - VERBOSE
                        type: string
                    type: object
                  metrics:
                    description: Metrics defines Prometheus/Mimir metrics configuration
                    properties:
                      enableServiceMonitor:
                        default: false
                        description: EnableServiceMonitor enables automatic ServiceMonitor
                          creation for Prometheus Operator
                        type: boolean
                      enabled:
                        default: true
                        description: Enabled enables metrics collection
                        type: boolean
                      interval:
                        default: 15s
                        description: Interval is the metrics scrape interval
                        type: string
                    type: object
                  profiling:
                    description: Profiling defines Pyroscope profiling configuration
                    properties:
                      enabled:
                        default: false
                        description: Enabled enables continuous profiling with Pyroscope
                        type: boolean
                      profiles:
                        description: Profiles defines which profile types to collect
                        properties:
                          cpu:
                            default: true
                            description: CPU enables CPU profiling
                            type: boolean
                          memory:
                            default: true
                            description: Memory enables memory profiling
                            type: boolean
                        type: object
                    type: object
                  tracing:
                    description: Tracing defines Grafana Tempo tracing configuration
                    properties:
                      enabled:
                        default: false
                        description: Enabled enables distributed tracing with Tempo
                        type: boolean
                      samplingRate:
                        default: "0.1"
                        description: SamplingRate is the trace sampling rate (0.0
                          - 1.0)
                        type: string
                    type: object
                type: object
              replicas:
                default: 1
                description: 'Replicas is the number of replicas (default: 1)'
                format: int32
                type: integer
              resources:
                description: Resources defines K8s resource requests/limits
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              storageUri:
                description: StorageURI is the S3/GCS path to model repository
                type: string
              tritonConfig:
                description: TritonConfig defines the Triton server configuration
                properties:
                  backendType:
                    description: 'BackendType is the backend type: python, tensorflow,
                      pytorch, etc.'
                    enum:
                    - python
                    - tensorflow
                    - pytorch
                    - onnxruntime
                    - tensorrt
                    type: string
                  image:
                    default: nvcr.io/nvidia/tritonserver
                    description: 'Image is the Triton container image (default: nvcr.io/nvidia/tritonserver)'
                    type: string
                  parameters:
                    description: Parameters are Triton runtime parameters
                    items:
                      description: TritonParameter defines a Triton runtime parameter
                      properties:
                        name:
                          description: Name is the parameter name
                          type: string
                        value:
                          description: Value is the parameter value
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  python_backend:
                    description: PythonBackend defines Python backend specific settings
                    properties:
                      extraArgs:
                        additionalProperties:
                          type: string
                        description: ExtraArgs are additional args passed to model
                          initialize()
                        type: object
                      shmDefaultByteSize:
                        default: 1048576
                        description: ShmDefaultByteSize is the shared memory size
                          in bytes
                        format: int64
                        type: integer
                    type: object
                  tag:
                    default: 24.12-py3
                    description: Tag is the image tag
                    type: string
                type: object
            required:
            - applicationRef
            - storageUri
            - tritonConfig
            type: object
          status:
            description: status defines the observed state of KalypsoTritonServer
            properties:
              availableReplicas:
                description: AvailableReplicas is the number of available replicas
                format: int32
                type: integer
              conditions:
                description: Conditions represent the current state of the KalypsoTritonServer
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deploymentName:
                description: DeploymentName is the name of created K8s Deployment
                type: string
              message:
                description: Message is a human-readable status message
                type: string
              phase:
                description: 'Phase represents the current phase: Pending, Running,
                  Failed'
                enum:
                - Pending
                - Running
                - Failed
                type: string
              serviceEndpoint:
                description: ServiceEndpoint is the Service endpoint URL
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      scale:
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.availableReplicas
      status: {}
